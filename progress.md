# Progress

This file is intentionally minimal to reduce session context.

Use `.pi/current-context.md` for current project state and token-budget rules.

Historical implementation details remain in ignored private specs/review files and `subagent-artifacts/`; read them only when a task explicitly requires that history.
